# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง (ห้าม commit .env ขึ้น GitHub)


PORT=

DB_HOST=localhost
DB_USER=
# ⚠️ ยังต้องกรอกเอง — ดูรหัสผ่านที่ http://nindam.ddns.net/web/
DB_PASSWORD=
DB_NAME=

# สุ่มไว้ให้แล้ว (64 hex chars) ใช้ได้เลยไม่ต้องเปลี่ยน
JWT_SECRET=
JWT_EXPIRES=
